type Version {
  name: String!
  version: String!
}

enum PrismaStatus {
  ok
  nok
}

type Status {
  prisma: PrismaStatus!
}

# --------------------------------------------------------------------
# QUERY
# --------------------------------------------------------------------
type Query {
  version: Version!
  status: Status!
}

# --------------------------------------------------------------------
# MUTATION
# --------------------------------------------------------------------
